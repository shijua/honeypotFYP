[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: pure function unit tests
    component: component tests with fake adapters
    adapter: infrastructure adapter tests (testcontainers)
    contract: schema and API contract tests
    e2e_smoke: minimal end-to-end smoke tests
asyncio_mode = auto
